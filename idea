score, blacklist, remove

do the handling in behaviour

create strucutres and api for gossip with params and threasholds based on an actual protocol

rpc:
    - account_info
    - create_account
    - transfer_funds
    - account_balance
    - account_history
    - transaction_block (find the block where a transaction occured)
    - transaction_proof (using merkle tree to return merkle proof)
