each node has its own mempool/cache and proposes the blocks himself
(should mempool/cache really be a hashmap ordered by timestamp?)

longest chain like bitcoin (to handle forks -> smallest hash)
only recognize a transaction is executed after 8 blocks are in front.
no money only bids (start/stop auction, bid)


dht:
blockchain on join -> boot node gives the chain
delete all logic regarding stores store
dht for peer discovery


quando um node faz bootstrap manda tb uma gRPC a pedir a blockchain e constroi o seu estado com a resposta.
o bootstrap node quando recebe uma connecção (connection established) envia a blockchain ao peer.
o peer dá stall enquanto espera a resposta -> simples await
o bootstrap node envia a blockchain assincronamente.
